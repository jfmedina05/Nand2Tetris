/** 
 *Jaiden Medina
 * Opp of Dmux
 * Multiplexor. If sel==1 then out=b else out=a.
 */

CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
	Not(in=sel,out=nsel);
    	And(a=a,b=nsel,out=c);
	And(a=b,b=sel,out=d);
	Or(a=c,b=d,out=out);
}
